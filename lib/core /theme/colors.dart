import 'package:flutter/material.dart';

class AppColors {
  // Surfaces
  static const background = Color(0xFF0A0C11);
  static const surface = Color(0xFF12151D);
  static const surfaceElevated = Color(0xFF1A1E28);

  // Borders
  static const border = Color(0x12FFFFFF); // rgba(255,255,255,0.07)
  static const borderStrong = Color(0x26FFFFFF); // rgba(255,255,255,0.15)

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8A93A6);
  static const textTertiary = Color(0xFF565E70);

  // Accent (amber/gold)
  static const accent = Color(0xFFF7B031);
  static const accentStrong = Color(0xFFFFC65C);
  static const accentTint = Color(0x1FF7B031); // rgba(247,176,49,0.12)

  // Good / positive (EGP strengthening)
  static const good = Color(0xFF34D399);
  static const goodTint = Color(0x1F34D399); // rgba(52,211,153,0.12)

  // Critical / negative (EGP weakening)
  static const critical = Color(0xFFF2685C);
  static const criticalTint = Color(0x1FF2685C); // rgba(242,104,92,0.12)

  // Info
  static const info = Color(0xFF6EA8E0);
  static const infoTint = Color(0x1F6EA8E0); // rgba(110,168,224,0.12)
}
